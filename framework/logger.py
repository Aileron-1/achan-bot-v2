# 
def log(message, level='default'):
    # save datetime, msg, and level to file.
    # and still print the message
    print(message, file=)

    # would it be less intensive to keep the file open and just log it directly, instead of opening it every log?